(function () {
  const offerSlider = new Swiper('.offer-slider-container', {
    slidesPerView: 'auto',
    loop: true,
    grabCursor: true
  })

  const clientsSlider = new Swiper('.clients-container', {
    slidesPerView: 'auto',
    loop: true,
    grabCursor: true
  })
}());